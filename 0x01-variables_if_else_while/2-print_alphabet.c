#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n = a;

	for (n; n <= "z"; n++)
		putchar(n);

	return (0);
}
