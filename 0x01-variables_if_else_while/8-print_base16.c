#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
		putchar((char)n);

	for (n = 97; n <= 102; n++)
                putchar((char)m);

	putchar('\n');
	return (0);
}
