#include "main.h"
#include <stdio.h>

/**
 *mul - prints the alphabet ten times
 * @a : integer
 * @b : integer
 *
 * Return: Always 0 (Success)
*/
int mul(int a, int b)
{
	return (a * b);
}
